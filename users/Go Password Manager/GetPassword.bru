meta {
  name: GetPassword
  type: http
  seq: 5
}

get {
  url: http://localhost:8080/get_password?name=Spotify
  body: none
  auth: inherit
}

params:query {
  name: Spotify
}

headers {
  Authorization: {{TOKEN}}
}

settings {
  encodeUrl: true
  timeout: 0
}
