meta {
  name: Login
  type: http
  seq: 3
}

post {
  url: http://localhost:8080/log_in
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "username": "salma123",
    "password": "salmaRS2016@VA"
  }
}

tests {
  const data = res.body;
  bru.setEnvVar("TOKEN", data.token);
  
}

settings {
  encodeUrl: true
  timeout: 0
}
